Custom checkbox with an inline label — the consent control on the contact form. Themed gold/blue tick.

```jsx
<Checkbox id="consent" required>
  Ho letto l'informativa privacy e acconsento al trattamento dei miei dati.
</Checkbox>
```

Pass standard input attributes (`required`, `checked`, `onChange`). The label content wraps to multiple lines cleanly.
