Large Fraunces editorial pull quote with a mono uppercase attribution. Place inside a tinted band (`background: var(--ink-3)`) for the full effect.

```jsx
<section style={{background:'var(--ink-3)', padding:'120px 0'}}>
  <div className="wrap">
    <PullQuote cite="— Il principio di ivEyes">
      Tracciabilità completa <em>senza imporre rigidità</em> al flusso di lavoro.
    </PullQuote>
  </div>
</section>
```
