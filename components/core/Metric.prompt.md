A big Fraunces numeral with a supporting label — the hero metric strip and product benefit stats.

```jsx
<Metric value="100%" label="Tracciabilità di processo per la conformità GMP · Annex 11" />
<Metric value="1 → N" label="Una piattaforma, più scenari" tone="teal" />
```

Tones: `gold` (theme accent — company hero) · `teal` (product KPIs). Value accepts any short node — percentages, arrows, abbreviations.
